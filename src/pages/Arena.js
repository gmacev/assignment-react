import React from 'react';
import CharacterPanel from "../components/CharacterPanel";

const Arena = () => {
    return (
        <div>
            <CharacterPanel/>
        </div>
    );
};

export default Arena;